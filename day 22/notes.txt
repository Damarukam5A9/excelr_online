splict():
+++++++++
       -it is used to add or remove elements from an array
       -it returns an array
       -it changes the original array

syntax:

array.splice(start,deletecount,element,element1,element2,element3,.....elememntN);
start
deletecount:it is used to specify the number of elements to be removed
element1









syntax:  

array.some((element,index,array)=>{
    //condition
    return element;
})